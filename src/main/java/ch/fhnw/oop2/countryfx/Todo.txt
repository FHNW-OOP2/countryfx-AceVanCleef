Todo list of CountryFX:

        must have:
        //Todo: Bindings @CountryOverview, CountryEditor (and ListView) (still relevant?)

        Important (lvl01)
            RootPM:
                - refractoring to selection handling with CountryPM proxyCountry and binding in RootPM                                      [done]
                - add ObservableList<ContinentPM> selectedContinents //Covering multiple cases: Europa; Europa,Asien; Asien,Europa;
                - add value change listener to proxyCountry in order to recalculate the property values of selectedContinents
                  (The value change listener on allCountries is fine since it reacts on add(Country) remove(country) )

        1) ListView
            a) Custom Cell Factory ''//Does not work. How to get pm's allCountries?'
            b) setupBindings(): //countryNameField.textProperty().bindBidirectional(country.nameProperty()); //it can't find nameProperty(). Why?
            c) flag by url
            d) current selected item (to RootPM's selectedCountryId)
        2) Footer
            a) bindings / value change updates (see RootPM's allContinent)
        3) CountryOverview
            a) connection / binding to selectedCountryIdProperty
            b) flag (use iso_2 + url-string) -> image = new Image(imagePath + pm.getProxyCountry().getIso_2() + ".png");
            Tipp: Centralize it + move logic to PM: write helper method getFlagImgUrl(int imgSize) within RootPM using StringBuilder and move imagePath constant to RootPM
        4) CountryEditor
            a) connection / binding to selectedCountryIdProperty
        5) Header
            a) Save to File (incl. save() @RootPM and @Service)

        nice to have:
